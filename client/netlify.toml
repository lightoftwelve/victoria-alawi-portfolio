[build]
  command = "npm run build"
  functions = "../netlify/functions"
  publish = "client/dist"

[dev]
  command = "npm run develop"
  publish = "client/dist"
  framework = "#custom"
  targetPort = 3000
  port = 8888
